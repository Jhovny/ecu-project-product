server:
  port: 8088

spring:
  application:
    name: product

  datasource:
    url: jdbc:postgresql://localhost:5433/db_product
    username: postgres
    password: '123456'

management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: "*"